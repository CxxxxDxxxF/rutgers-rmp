{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.worker",
    "watchPatterns": [
      "Dockerfile.worker",
      "package.json",
      "package-lock.json",
      "railway.ai-collector.json",
      "worker/**"
    ]
  },
  "deploy": {
    "startCommand": "npm run worker:ai",
    "cronSchedule": "*/10 * * * *",
    "restartPolicyType": "NEVER"
  }
}
